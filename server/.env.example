# 🔒 SECURE ENVIRONMENT VARIABLES
# Replace with new MongoDB password after changing it in Atlas

# Database Configuration
MONGODB_URI=mongodb+srv://gandhiyaash:YOUR_NEW_SECURE_PASSWORD@cluster0.ztwrafd.mongodb.net/bitcoin-policy-forms?retryWrites=true&w=majority&appName=Cluster0

# Server Configuration
PORT=5001
NODE_ENV=development

# Security Configuration
ADMIN_API_KEY=your-secure-admin-key-here-change-this

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://bitcoinpolicy.in,https://www.bitcoinpolicy.in

# INSTRUCTIONS:
# 1. Replace YOUR_NEW_SECURE_PASSWORD with the new password from MongoDB Atlas
# 2. Replace your-secure-admin-key-here-change-this with a strong random string
# 3. Add these same variables to Railway with NODE_ENV=production
